import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Favorites'  
}


const favorites = () => {
  return (
    <div>
      <div>Favorites</div>
    </div>
  )
}

export default favorites
